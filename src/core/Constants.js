const DATASET_KEYS = {
    id: "epicModalId",
    config: "epicModalConfig",
    closeButton: "epicModalCloseButton"
}

const HTML_DATA_KEYS = {
    id: "data-epic-modal-id",
    config: "data-epic-modal-config",
    closeButton: "data-epic-modal-close-button"
}

const ANIMATIONS = {
    "default":"default",
}

const THEMES = {
    "default-light":"default-light",
    "default-dark":"default-dark",
    "custom":"custom"
}

const DEFAULT_Z_INDEZ = 999999999;

export {
    DATASET_KEYS,
    HTML_DATA_KEYS,
    THEMES,
    ANIMATIONS,
    DEFAULT_Z_INDEZ 
}